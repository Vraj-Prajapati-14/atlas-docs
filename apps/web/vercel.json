{
  "framework": "nextjs",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @atlas/types build && pnpm --filter @atlas/utils build",
  "buildCommand": "next build",
  "outputDirectory": ".next"
}
